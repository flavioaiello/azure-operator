# Azure Landing Zone Operator
#
# Core dependencies for production runtime.
# Pinned versions for reproducible builds.

# Pydantic for typed models with validation
pydantic>=2.5.0,<3.0.0

# Azure SDK for Python
azure-identity>=1.15.0,<2.0.0
azure-mgmt-resource>=23.0.0,<24.0.0
azure-mgmt-resourcegraph>=8.0.0,<9.0.0
azure-core>=1.29.0,<2.0.0

# YAML parsing
PyYAML>=6.0.1,<7.0.0

# CLI framework
click>=8.1.0,<9.0.0
